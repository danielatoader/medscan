import { Text, SafeAreaView } from "react-native";

const PatientMedScreen: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Patient med</Text>
    </SafeAreaView>
  );
};

export default PatientMedScreen;
export const name = "Patient";
